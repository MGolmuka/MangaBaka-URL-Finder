Privacy policy

- The extension processes the active MangaBaka series page title/metadata to perform user-requested source matching.

- Settings and cached lookup data are stored locally in chrome.storage.local.

- The extension sends lookup queries only to the sites needed to perform the search.

- The developer does not sell user data and does not use it for advertising.